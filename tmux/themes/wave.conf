# Kanagawa Wave (dark) — sourced by scripts/theme.sh in dark mode.
%hidden BG="#1f1f28"
%hidden FG="#dcd7ba"
%hidden GREY="#54546d"
%hidden BLUE="#7e9cd8"
%hidden GREEN="#98bb6c"
%hidden YELLOW="#e6c384"

set -g status-style "bg=${BG},fg=${FG}"
set -g pane-border-style "fg=${GREY}"
set -g pane-active-border-style "fg=${BLUE}"
set -g message-style "bg=${YELLOW},fg=${BG}"
set -g mode-style "bg=${BLUE},fg=${BG}"

# Left: session name (turns yellow while the prefix is held).
set -g status-left "#[bg=#{?client_prefix,${YELLOW},${BLUE}},fg=${BG},bold] #S #[bg=${BG},fg=${BG}] "
# Right: mode glyph + date + time.
set -g status-right "#[fg=${YELLOW}]#(~/.config/tmux/scripts/theme.sh) #[fg=${GREY}]%a %d %b #[fg=${FG}]%H:%M "

setw -g window-status-format "#[fg=${GREY}] #I #W "
setw -g window-status-current-format "#[bg=${GREEN},fg=${BG},bold] #I #W "
